# 31. FILE UPLOAD SECURITY CONTROLS

## 1. Protections Enforced
- Strict extension filtering (`.jpg`, `.jpeg`, `.png`, `.webp`).
- 50MB file size ceiling.
- Basename path sanitization preventing directory traversal.
