# 11. PUBLISH VERSIONING & REVISION CONTROL

## 1. Version Tracking
- Every publish increments `publishedVersion` (e.g. `v1.0`, `v1.1`).
- Historical revisions retain complete immutable asset snapshots.
